@page "{id}"
@model PrintBucket.Web.Pages.BucketModel
@inject Microsoft.Extensions.Localization.IStringLocalizer<SharedResource> L
@{
    ViewData["Title"] = L["BucketViewTitle"];
}

<div class="container py-5">
    <div class="row">
        <div class="col-12">
            @if (Model.Bucket != null)
            {
                <div class="card shadow-sm mb-4">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center mb-4">
                            <h1 class="display-6 mb-0">@Model.Bucket.Name</h1>
                            <span class="badge bg-primary btn-app d-none">@Model.Bucket.Status</span>
                        </div>
                        <p class="text-muted">
                            <i class="bi bi-calendar3"></i> @L["CreatedAt"]: @Model.Bucket.CreatedAt.ToLocalTime().ToString("g")
                        </p>
                        <!-- QR code for bucket.id -->
                        <div class="mb-3">
                            <div id="bucket-qr"></div>
                            <div class="small text-muted mt-2">@Model.Bucket.Id</div>
                        </div>
                    </div>
                </div>

                @if (Model.Images?.Any() == true)
                {
                    <div class="row g-4">
                        @foreach (var image in Model.Images)
                        {
                            <div class="col-12 col-md-6 col-lg-4">
                                <div class="card h-100">
                                    <img src="@image.SmallKey" class="card-img-top" alt="@image.FileName" loading="lazy" />
                                    <div class="card-body">
                                        <h5 class="card-title text-truncate">@image.FileName</h5>
                                        <p class="card-text small text-muted">
                                            @image.CreatedAt.ToLocalTime().ToString("g")
                                        </p>
                                        <div class="btn-group">
                                            <a href="@image.OriginalKey" class="btn btn-sm btn-outline-primary" target="_blank">
                                                <i class="bi bi-download"></i> @L["Download"]
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                    </div>
                }
                else
                {
                    <div class="text-center py-5">
                        <p class="text-muted"></p>
                    </div>
                }

                <!-- File uploader at the bottom -->
                <div class="fixed-bottom d-flex justify-content-center pb-4">
                    <form method="post" enctype="multipart/form-data" asp-page-handler="Upload" asp-route-id="@Model.Bucket.Id">
                        <input type="file" id="fileInput" name="files" multiple class="d-none" />
                        <label for="fileInput" class="btn btn-lg btn-success rounded-circle" style="width:64px;height:64px;display:flex;align-items:center;justify-content:center;">
                            <i class="bi bi-plus-lg fs-2"></i>
                        </label>
                    </form>
                </div>
            }
            else
            {
                <div class="text-center py-5">
                    <h2>@L["BucketNotFound"]</h2>
                    <p class="text-muted">@L["BucketNotFoundDesc"]</p>
                    <a href="@Url.Page("/Index")" class="btn btn-primary">@L["BackHome"]</a>
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            var bucketId = "@Model.Bucket?.Id";
            if (bucketId) {
                new QRCode(document.getElementById("bucket-qr"), {
                    text: bucketId.replace("bucket-",""),
                    width: 128,
                    height: 128,
                    colorDark: "#000000",
                    colorLight: "#ffffff",
                    correctLevel: QRCode.CorrectLevel.H
                });
            }

            // Auto-submit form when files are selected
            var fileInput = document.getElementById('fileInput');
            if (fileInput) {
                fileInput.addEventListener('change', function () {
                    fileInput.form.submit();
                });
            }
        });
    </script>
}