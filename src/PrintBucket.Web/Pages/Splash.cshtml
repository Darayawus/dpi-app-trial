@page
@{
    ViewData["Title"] = "Welcome";
}

<div class="d-flex align-items-center justify-content-center" style="height: calc(100vh - 120px);">
    <div class="text-center">
        <h1 class="display-4">PrintBucket</h1>
        <p class="lead">Welcome — you will be redirected in <span id="count">5</span> seconds.</p>
        <p><a class="btn btn-primary" asp-page="/Index">Go now</a></p>
    </div>
</div>

@section Scripts {
<script>
    (function(){
        var seconds = 5;
        var el = document.getElementById('count');
        // Navigate to the root adding skipSplash=1 to avoid the redirect middleware
        var url = '@Url.Page("/Index", new { skipSplash = "1" })';
        var t = setInterval(function(){
            seconds--;
            if (el) el.textContent = seconds;
            if (seconds <= 0) {
                clearInterval(t);
                window.location.href = url;
            }
        }, 1000);
    })();
</script>
}