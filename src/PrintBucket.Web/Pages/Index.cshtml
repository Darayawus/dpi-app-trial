@page
@model IndexModel
@using PrintBucket.Web
@inject Microsoft.Extensions.Localization.IStringLocalizer<SharedResource> L
@{
    ViewData["Title"] = L["HomeTitle"];
}

<div class="full-bleed full-viewport index-hero">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-12 col-md-10 col-lg-8 text-center">
                <!-- Hero image optimized for mobile -->
                <div class="hero-image-wrapper mb-5">
                    <img src="~/img/index_img.png" alt="@L["ImageAlt"]" class="img-fluid" />
                </div>

                <!-- Main content with improved spacing and hierarchy -->
                <div class="hero-content">
                    <h1 class="display-6 mb-4">@L["HeroHeadline"]</h1>
                    
                    <p class="lead mb-4">
                        @L["HeroSubhead"]
                    </p>

                    <!-- Benefits with clearer separation -->
                    <div class="benefits-wrapper mb-5">
                        <p class="text-secondary">
                            <span class="benefit-item">@L["HeroBenefits"]</span>
                            <span class="separator mx-2">•</span>
                            <span class="benefit-item">@L["QRCodeNote"]</span>
                            <!--<span class="separator mx-2">•</span>
                            <span class="benefit-item">@L["PrintsNote"]</span>
                            -->
                        </p>
                    </div>

                    <!-- More prominent CTAs -->
                    <div class="cta-wrapper d-flex justify-content-center align-items-center gap-3">
                        <a class="btn-start btn-lg d-inline-flex align-items-center justify-content-center" 
                           style="min-width: 150px"
                           href="@Url.Page("/Create")">
                            @L["Start"]
                        </a>
                        <a class="btn-access btn-lg d-inline-flex align-items-center justify-content-center" 
                           style="min-width: 150px"
                           href="@Url.Page("/Access")">
                            @L["Access"]
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        (function() {
            var seconds = 10;
            var targetUrl = '@Url.Page("/Privacy")';
            var timer = setInterval(function() {
                seconds--;
                if (seconds <= 0) {
                    clearInterval(timer);
                    //window.location.href = targetUrl;
                }
            }, 1000);
        })();
    </script>
}